---
# vars file for git-download-mantis

#####     Required Variables      #####
# Fill in appropriate values below unless passing from a higher precedence

# Choose the git tagged release that you would like to download and install
tagged_release_version: "release-2.15.0"


#####     Optional Variables      #####

# These variables are set with the below defaults in defaults/main.yml. 
# Only uncomment and change them or pass values if you would like

# The git repo for the Mantis Bug Tracker (BT)
#git_repo: "https://github.com/mantisbt/mantisbt.git"

# The DB user the application will use to connect.
#db_username: 'mantisdbuser'

# The DB password used by the db_username value to connect to the database
#db_password: ''

# The name of the database to create and use for Mantis BT
#database_name: 'bugtracker'

# The Document Root or file path where Mantis files will be stored and served up by your webserver. The default path is "/var/www/html" and assumes you are running Apache2 on Debian or Ubuntu
#web_files_path: "/var/www/html"

# The linux username used by your webserver. The default value is "www-data" which assumes Apache is used on a Debian or Ubuntu linux
#web_user: "www-data"

# The linux group used by your webserver. The default value is "www-data" which assumes Apache is used on a Debian or Ubuntu linux
#web_group: "www-data"